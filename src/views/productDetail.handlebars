<h1>{{title}}</h1>

<p>{{description}}</p>
<p>Precio: ${{price}}</p>
<p>Stock disponible: {{stock}}</p>
<p>Categoría: {{category}}</p>

<form action="/carts/{{cartId}}/product/{{_id}}" method="POST">
  <button type="submit">Agregar al carrito</button>
</form>

<a href="/products">Volver al listado</a>
